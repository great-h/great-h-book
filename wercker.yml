box: eiel/review
build:
  steps:
    - bundle-install
    - script:
        name: rake deploy
        code: bundle exec rake deploy
deploy:
  steps:
    - bundle-install
    - script:
        name: rake deploy:latest
        code: bundle exec rake deploy:latest
